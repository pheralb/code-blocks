---
title: Code Block MDX with Shiki
description: Create a Code Block component for MDX using Shiki for syntax highlighting.
category: [React, MDX, Shiki, Components]
---

<Showcase>

```tsx
import type { ComponentProps } from "react";

const MyComponent = (props: ComponentProps<"div">) => {
  return <div {...props}>Hello, World!</div>;
};

export default MyComponent;
```

</Showcase>

## Installation

### shadcn/ui

<CopyShadcnCommand name="mdx-shiki" />

### Manual

Before creating the MDX component, make sure you have the basic component structure:

<DocCard folder="react" document="code-block" />

1. Install the following dependencies:

<CodeBlockSelectPkg title="MDX Types" type="install" command="@types/mdx -D" />

2. Create the Copy Button component:

<DocCard folder="react" document="copy-button" />

3. Create a `react-to-text` utility to get plain text from `pre` elements:

<ShowSource component="react-to-text" />

## Highlighter Setup

1. Setup `data-title` & `data-language` properties:

<DocCard folder="shiki" document="add-properties" />

2. Setup Shiki highlighter:

<DocCard folder="shiki" document="highlighter" />

3. Setup Shiki Markdown integration:

<DocCard
  folder="shiki"
  document="highlighter"
  anchor="markdownmdx-integration"
/>

4. Create your `PreShikiComponent` component:

<ShowSource component="mdx-shiki" />

5. Usage:

In your MDX Components object, add the new `PreShikiComponent` component:

```ts {3,6}
import type { MDXComponents as MDXComponentsType } from "mdx/types";

import { PreShikiComponent } from "@/components/code-block/pre-shiki";

const MDXComponents: MDXComponentsType = {
  ...PreShikiComponent,
};
```
